# --- Secrets ---
.env
*.env
**/THETADATA_API_KEY*
secrets.*

# --- data/ holds ThetaData downloads + run outputs (local cache, not in git). ---
# Tracked exceptions: README, inventory manifest, calendar rules — see data/README.md
data/*
!data/mbh_returns/
!data/README.md
!data/inventory/
!data/inventory/**
!data/calendar/
!data/calendar/**
*.parquet
*.log
data/logs/

# Dashboard data blob IS tracked (it is the Pages deploy artifact): see
# dashboard/data/dashboard_data.json (not under data/, so not ignored).

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# --- Editor/OS ---
.DS_Store
Thumbs.db
.vscode/
.idea/
installers/

# Local Agent Memory Mesh junctions (see Projects/.agent-memory)
.agent-memory/
.agent-memory-upstream/
